[project]
name = "symphony-compiler"
version = "0.1.0"
description = "Melody to Symphony assembly compiler."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Symphony" }]
license = { text = "MIT" }

[project.scripts]
melc = "melc.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
